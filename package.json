{
  "name": "page-statistics-service",
  "description": "Service returning statistics based on a requested page",
  "version": "0.1.0",
  "main": "page-statistics-service.js",
  "author": "IDKWID",
  "license": "ISC",
  "scripts": {
    "test": "mocha",
    "deploy": "node tasks.js deploy",
    "publish": "node tasks.js publish"
  },
  "devDependencies": {
    "aws-lambda-toolkit": "^0.4.0",
    "mocha": "^5.0.4",
    "promised-lambda-context": "^0.1.0",
    "should": "^13.2.1"
  },
  "dependencies": {
    "aws-sdk": "^2.209.0",
    "promised-dynamo": "^0.12.0"
  }
}
